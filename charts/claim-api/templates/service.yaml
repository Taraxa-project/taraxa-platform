apiVersion: v1
kind: Service
metadata:
  name: {{ include "claim-api.fullname" . }}
  labels:
    {{- include "claim-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.serverPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "claim-api.selectorLabels" . | nindent 4 }}
