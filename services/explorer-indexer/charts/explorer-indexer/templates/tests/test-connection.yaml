apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "explorer-indexer.fullname" . }}-test-connection"
  labels:
    {{- include "explorer-indexer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "explorer-indexer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
